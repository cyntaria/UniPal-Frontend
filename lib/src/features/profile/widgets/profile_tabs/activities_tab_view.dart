import 'package:flutter/material.dart';

class ActivitiesTabView extends StatelessWidget {
  const ActivitiesTabView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
