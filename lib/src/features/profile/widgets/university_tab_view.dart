import 'package:flutter/material.dart';

class UniversityTabView extends StatelessWidget {
  const UniversityTabView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
